<p id="notice"><%= notice %></p>

<h1>IP Addresses</h1>

<p>
  <%= link_to 'New Ip', new_ip_path, class: "btn btn-secondary btn-sm" %>
  <%= link_to 'Ping', ips_path(ping: true), class: "btn btn-info btn-sm" %>
</p>

<% unless @view %>
  <%= render "list_view" %>
<% else %>
  <%= render "organized_view" %>
<% end %>

<br>

<%= link_to 'New Ip', new_ip_path, class: "btn btn-secondary btn-sm" %>
