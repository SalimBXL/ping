<p id="notice"><%= notice %></p>

<h1>IP Addresses</h1>

<div class="clearfix">
  <span class="float-left">
    <p>
      <%= link_to 'New Ip', new_ip_path, class: "btn btn-secondary btn-sm" %>
    </p>
  </span>
  <span class="float-right">
    <p>
      <% link_to 'Ping all!', ips_path(ping: true), class: "btn btn-info btn-sm" %>
    </p>
  </span>
</div>


<% unless @view %>
  <%= render "list_view" %>
<% else %>
  <%= render "organized_view" %>
<% end %>

<br>

<%= link_to 'New Ip', new_ip_path, class: "btn btn-secondary btn-sm" %>
